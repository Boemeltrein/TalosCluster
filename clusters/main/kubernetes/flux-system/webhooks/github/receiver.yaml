apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: webapp
  namespace: flux-system
spec:
  type: github
  events:
    - "ping"
    - "push"
  token: ${WEBHOOK_GITHUB_TOKEN}
  resources:
    - kind: GitRepository
      name: cluster
      namespace: flux-system
