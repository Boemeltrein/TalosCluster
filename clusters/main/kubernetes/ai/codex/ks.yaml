apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: codex
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/ai/codex/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

