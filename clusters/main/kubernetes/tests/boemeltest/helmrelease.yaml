---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy
spec:
  interval: 1h
  chart:
    spec:
      chart: dummy
      version: "0.0.0"
      sourceRef:
        kind: HelmRepository
        name: dummy
        namespace: flux-system
  install:
    disableWait: true
  upgrade:
    disableWait: true
  suspend: true