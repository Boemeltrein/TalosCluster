apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: it-tools
  namespace: it-tools
spec:
  interval: 5m
  chart:
    spec:
      chart: it-tools
      version: 8.13.13
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 5m
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    global:
      stopAll: false

    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: it-tools.${BASE_DOMAIN}
        integrations:
          certManager:
            enabled: true
            certificateIssuer: cloudflare-staging
    image:
      repository: oci.trueforge.org/containerforge/it-tools
      tag: sandbox@sha256:c54d3f4f15e64a09facb084ea76d492e4a32680632f3cc78a61769c25a7248ee
      pullPolicy: IfNotPresent

#    workload:
#      main:
#        podSpec:
#          containers:
#            main:
#              command:
#                - "/bin/sh"
#                - "-c"
#                - "sleep infinity"

#    workload:
#      main:
#        podSpec:
#          initContainers:
#            init-mkdir:
#              enabled: true
#              type: init
#              imageSelector: "image"
#              command:
#                - /bin/sh
#                - -c
#              args:
#                - mkdir -p /tmp/nginx-temp

#    persistence:
#      config:
#        enabled: true
#        targetSelectAll: true
#        mountPath: /config

#    persistence:
#      tmp:
#        enabled: false

#    workload:
#      main:
#        podSpec:
#          containers:
#            main:
#              securityContext:
#                readOnlyRootFilesystem: false

    service:
      main:
        ports:
          main:
            protocol: http
            targetPort: 8080
            port: 8080