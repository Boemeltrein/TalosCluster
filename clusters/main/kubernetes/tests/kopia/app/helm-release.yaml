apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kopia
  namespace: kopia
spec:
  interval: 15m
  chart:
    spec:
      chart: kopia
      version: 27.1.2
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  timeout: 20m
  maxHistory: 3
  driftDetection:
    mode: warn
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  
  values:
    TZ: Europe/Amsterdam
    
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: kopia.${BASE_DOMAIN}
        integrations:
          certManager:
            enabled: true
            certificateIssuer: cloudflare-staging

    image:
      repository: oci.trueforge.org/containerforge/kopia
      tag: 0.22.3@sha256:dfe0477f071a4ab9489bc8e75a350afdf50750251ecad2ed983b903b2fda91cc
      pullPolicy: IfNotPresent
    service:
      main:
        ports:
          main:
            protocol: http
            port: 51515
    persistence:
      config:
        enabled: true
        mountPath: "/config"
      cache:
        enabled: true
        mountPath: "/config/cache"
      logs:
        enabled: true
        mountPath: "/config/logs"
      rclone:
        enabled: true
        mountPath: "/config/rclone"

